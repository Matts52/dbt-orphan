name: "dbt_orphan_integration_tests"
version: "0.1.0"

config-version: 2
require-dbt-version: [">=1.1.0", "<3.0.0"]

profile: "integration_tests_postgres"

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]
